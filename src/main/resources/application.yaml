spring:
  application:
    name: ucamp-api

  datasource:
    url: jdbc:postgresql://db.pzfdxqklbhnnyciwtdip.supabase.co:5432/postgres
    username: postgres
    password: Daehwa310@@
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect


jwt:
  secret: "THIS_IS_SUPER_LONG_SECRET_0123456789abcdef0123456789abcdef"
  access-token-exp-minutes: 60

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.tool.schema: debug
